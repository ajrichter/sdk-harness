# REST API base URL.
# This is the canonical source of truth the migration harness must detect and replace.
api.base-url=https://api.example.com

# Endpoint paths — kept separate so the harness can match path patterns independently
api.users.path=/api/v1/users
api.posts.path=/api/v1/posts
